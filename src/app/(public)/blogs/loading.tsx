import Loading from "@/components/ui/Loading";

const BlogsLoadingPage = () => {
    return (
        <div className="pt-30">
            <Loading/>
        </div>
    );
};

export default BlogsLoadingPage;